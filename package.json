{
  "name": "well-cost-per-meter",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:css": "sass sass/style.sass css/style.css --style=expanded",
    "watch:css": "sass sass/style.sass css/style.css --watch"
  },
  "devDependencies": {
    "sass": "^1.77.0"
  }
}
